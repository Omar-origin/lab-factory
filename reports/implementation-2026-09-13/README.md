# Lab Factory 计划执行交付

> 后续发布状态：2026-09-14 网站已部署，12 项线上检查通过，详见 `../deployment-2026-09-14/README.md`。客户端正式发布仍待完成；下文保留本地实现交付时的状态。

更新：2026-09-14。基于工作区 HEAD `dc0986e` 实施；本轮修改保存在本地，未提交、推送或部署。原始用户报告没有修改。

## 已落地

| 工作包 | 实现与边界 |
| --- | --- |
| A 本地质量证据 | 新增 artifact_store，登记真实文件及 SHA-256；strict/balanced 共同使用 verify_draft 的本机审计。确认前、最终接受前重新验证文件与已登记依赖。模型填写的 pass 不再足以通过。|
| B 计次与恢复 | 参数/会话创建成功后才请求计次；新 strict/balanced 会话使用同一稳定 report_id。pending/retry_required/acknowledged 记录支持超时后以原编号重试，服务端幂等记录仍是权威。没有修改历史扣次记录。|
| C 原生图表 | v3 内容包接入正式引擎和 MCP。导入真实 PNG/JPEG，核对哈希，按容器限宽等比嵌入；生成可编辑 Word 三线表、重复表头、独立图表编号和正文引用。保护原模板与未目标化的 DOCX 部件。复杂固定高度行、多栏和无法确定宽度的位置明确阻止自动插入。|
| D 内容与差异 | 区分真实图表与占位；未补证据不能完整交付。新原生正文使用标记隔离固定模板、题注和表头的文本相似度影响；仅结构相近改为提示。检测“本变体”等生成说明。按任务 required_artifacts 检查构件映射和声明类型；事实清单登记来源。|
| E 多宿主 | WorkBuddy/Kimi 的 stdio 配置导出；通用 `tool --args-file` CLI 桥接；独立报告本地文件、模型视觉、桌面截图能力，未探测的不宣称支持。|
| F 网站 | 使用文档填入准备材料、安装/激活、第一条指令、补图、编辑和失败恢复；入口展示自建 DOCX 样稿；手机账单入口与登录后套餐/抵扣偏好恢复；面板提供下一步；说明云端模型可能接收宿主发送的材料。|
| G 测试和预览构建 | 新增 13 项原生图表/证据/计次/端到端回归，加入本地 test 命令和 CI。生成 macOS arm64 本地预览可执行文件，验证打包安全边界。|

## 文件

- `demo/native-report.docx`：本轮正式引擎生成的可编辑样稿。
- `demo/native-report.pdf`：LibreOffice 渲染预览；已人工查看图片、三线表与编号。
- `demo/native-template.docx`、`demo/content-package.json`：模板与内容包示例。
- `validation-summary.json`：本地检查、样稿和可执行文件哈希。
- `test-results/`：本次测试输出。
- 仓库 `skills/lab-skill-factory/references/native-media-v3.md`：完整 MCP/CLI 接入及恢复说明。
- 仓库 `mcp/lab-skill-factory/dist/native-preview/lab-factory`：隔离构建的本地预览客户端，未替换旧客户端，未做 Developer ID 发布签名或公证。

公开样稿仅使用项目自己的说明文字、示例表格数据和官网截图，不是客户案例或学生实验结果。网站中的 DOCX 下载文件已经与本轮引擎产物同步；网站源码尚未上线。

## 验证结果

- Python：v1 20/20、v2 38/38、autopilot 18/18、新增原生流程 13/13，共 89 项通过。
- 新增回归包含：真实 Word 表格与模板部件保留、图片哈希错误、缺少引用、缺失证据、伪造审计、审阅前后文件变更、结构相近的不同正文、共享模板/表头排除、超时后同编号重试、balanced 原生生成到最终确认、模板单元格内表格、矩形数据校验。
- JavaScript 套餐测试通过：URL 恢复、非法套餐回退、登录返回地址、抵扣偏好、移动摘要。
- `npm run check` 通过；内容包与 v3 JSON Schema 匹配；Python 编译和 `git diff --check` 通过。
- 本地浏览器实看窄屏首页与教程；448px 宽度下教程无横向溢出，目录锚点均存在，生成流程锚点导航正常。未通过真实账号创建订单。
- 最终可执行文件安全检查通过：18 个允许的运行时文件；任意脚本调用、外部脚本/依赖覆盖均被阻止。

## 尚未完成、不能当成已证明的事项

1. Windows/macOS 的 Word/WPS 打开→编辑表格→保存→复查的完整矩阵。当前只是 OOXML/解析检查及 LibreOffice 可视预览，不能写成所有编辑器均实测兼容。
2. WorkBuddy、Kimi、DeepSeek 的真实账号、具体模型版本端到端测试；当前完成工具与桥接实现，没有把品牌名当成能力保证。没有新增绕过宿主权限的自动桌面截图器，缺少截图能力时使用人工提供的真实图片。
3. 36 份固定模型与材料的内容对照、人工盲评和返工时间统计。自动检查只能确认文件、已声明类型和来源字段，不能证明截图真伪、类图的视觉语义、个人经历真实性或整体写作质量。
4. 原计划提出的服务端 reservation/commit/release 没有另建一套计费协议。本版保留原“报告会话计次”的口径，用先创建后扣次、稳定编号和持久恢复意图修复误扣/重试路径；失败后的会话继续使用原编号。
5. 旧版 2.1 自动驾驶会话若缺少任务快照，会给出明确的旧会话提示；保留原目录，可用原客户端完成。没有批量迁移或改写用户历史会话。
6. 正式发布签名、公证、Windows 构建、真实激活端到端验收和生产网站发布未执行。当前产物是本地可审阅实现及预览构建。

下一步放行顺序：用真实宿主和目标 Office/WPS 各完成一次样稿往返 → 固定模型进行内容对照 → 正式构建、签名及激活验收 → 再发布网站与客户端。不要跳过已列出的证据边界。
